[project]
name = "knowledgec-privacy-audit"
version = "0.1.0"
description = "Inspect and clear macOS's knowledgeC.db activity-tracking database."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.black]
line-length = 88
target-version = ["py310"]
